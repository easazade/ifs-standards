{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ifs-standards.org/schemas/v1/entities/label.schema.json",
  "title": "Label",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "id": {
      "type": "string",
      "description": "Globally unique identifier for this label."
    },
    "ifsId": {
      "type": "string",
      "description": "IFS system identifier for this Label."
    },
    "entityType": {
      "type": "string",
      "const": "Label",
      "description": "Entity type discriminator. Always \"Label\" for Label entities."
    },
    "basedOn": {
      "type": "string",
      "description": "Id of the object this object is derived from."
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when this label was created."
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when this label was last updated."
    }
  },
  "required": ["id", "ifsId", "entityType", "createdAt", "updatedAt"]
}
