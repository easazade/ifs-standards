{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ifs-standards.org/schemas/v1/entities/rule.schema.json",
  "title": "Rule",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "id": {
      "type": "string",
      "description": "Globally unique identifier for this rule."
    },
    "ifsId": {
      "type": "string",
      "description": "IFS system identifier for this Rule."
    },
    "entityType": {
      "type": "string",
      "description": "Entity category for this object, normally Rule."
    },
    "basedOn": {
      "type": "string",
      "description": "Id of the object this object is derived from."
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when this rule record was created."
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when this rule record was last updated."
    }
  },
  "required": ["id", "ifsId", "entityType", "createdAt", "updatedAt"]
}
