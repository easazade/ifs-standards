{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ifs-standards.org/schemas/v1/entities/comment.schema.json",
  "title": "Comment",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "id": {
      "type": "string",
      "description": "Globally unique identifier for this comment."
    },
    "ifsId": {
      "type": "string",
      "default": "com.ifs.entity.comment",
      "description": "Stable IFS entity identifier for Comment."
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when this comment was created."
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when this comment was last updated."
    }
  },
  "required": ["id", "ifsId", "createdAt", "updatedAt"]
}
