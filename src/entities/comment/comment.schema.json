{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ifs-standards.org/schemas/v1/entities/comment.schema.json",
  "title": "Comment",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "id": {
      "type": "string",
      "description": "Globally unique identifier for this comment."
    },
    "ifsId": {
      "type": "string",
      "description": "IFS system identifier for this Comment."
    },
    "entityType": {
      "type": "string",
      "const": "Comment",
      "description": "Entity type discriminator. Always \"Comment\" for Comment entities."
    },
    "basedOn": {
      "type": "string",
      "description": "Id of the object this object is derived from."
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when this comment was created."
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when this comment was last updated."
    }
  },
  "required": ["id", "ifsId", "entityType", "createdAt", "updatedAt"]
}
