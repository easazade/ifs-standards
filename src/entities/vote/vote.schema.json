{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ifs-standards.org/schemas/v1/entities/vote.schema.json",
  "title": "Vote",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "id": {
      "type": "string",
      "description": "Globally unique identifier for this vote."
    },
    "ifsId": {
      "type": "string",
      "description": "IFS system identifier for this Vote."
    },
    "entityType": {
      "type": "string",
      "description": "Entity category for this object, normally Vote."
    },
    "basedOn": {
      "type": "string",
      "description": "Id of the object this object is derived from."
    },
    "entityDocumentationUrl": {
      "type": "string",
      "format": "uri",
      "description": "URL for documentation about this entity."
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when this vote record was created."
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when this vote record was last updated."
    },
    "decisionId": {
      "type": "string",
      "description": "Identifier of the decision this vote participates in."
    },
    "memberId": {
      "type": "string",
      "description": "Identifier of the member who cast or owns this vote."
    },
    "value": {
      "type": "string",
      "description": "Vote value recorded for the decision, such as an implementation-defined choice or consent signal."
    },
    "previousRevisionId": {
      "type": "string",
      "description": "Identifier of the previous revision of this vote, if this vote amends an earlier vote."
    }
  },
  "required": [
    "id",
    "ifsId",
    "entityType",
    "entityDocumentationUrl",
    "createdAt",
    "updatedAt",
    "decisionId",
    "memberId",
    "value"
  ]
}
